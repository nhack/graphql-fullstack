import {
  TodoServiceRest
} from '../../../../client/dev/todo/services/todo-service-rest';

describe('todo_service', () => {
  describe('creation', () => {
    it('should create the service correctly', () => {
		expect(true).toBe(true);
    });
  });
});
